/**
 * Array of objects {name, object} with information about custom namespaces and
 * its value
 * @const namespaces - array of namespaces that will be be in window scope.
 * @type {array<object>}
 */
const namespaces = [
  {
    name: 'M.plugin',
    object: '{}',
  },
];

module.exports = namespaces;
